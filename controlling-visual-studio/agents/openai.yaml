interface:
  display_name: "Control Visual Studio"
  short_description: "CLI, EnvDTE, and GUI control for Visual Studio"
  default_prompt: "Use $controlling-visual-studio with its scripts/ helpers to try CLI first, EnvDTE second, and Computer Use last for a Visual Studio project."

policy:
  allow_implicit_invocation: true
